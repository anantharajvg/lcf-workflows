# Globus transfer fixture

This directory contains a small, non-sensitive test dataset for exercising the
ACE IRI concept data-transfer workflow. It is not scientific input and must not
be used to validate a scientific result.

The dataset has two deterministic scalar observations. `SHA256SUMS` is generated
before each transfer and verified after arrival at the Odo Wolf2 destination.
